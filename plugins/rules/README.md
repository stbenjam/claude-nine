# rules

Context-specific repository rules for agents.

- `git-rules`: critical safeguards for remotes, pushing, rebasing, and branch comparisons
- `test-failures`: evidence-based investigation of failed tests and their causes
